services:
  postgres:
    image: postgres:16.3
    restart: unless-stopped
    network_mode: host
    expose:
      - 5432
    volumes:
      - type: bind
        source: ~/databases/postgres/
        target: /var/lib/postgresql/data
    env_file: env
